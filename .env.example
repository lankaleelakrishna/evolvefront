# Base URL of the backend API. Copy this file to .env.local (or set it in
# Vercel's project environment variables) and point it at your deployed
# backend once it is publicly reachable. Falls back to http://localhost:8080
# for local development when unset.
REACT_APP_API_URL=http://localhost:8080
